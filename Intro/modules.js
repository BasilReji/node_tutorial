// Modules - Encapsulated code (only share minimum) Functions

// CommonJS, every file is module (by default)

const sayHi = (name) => {
  console.log(`Hello there, ${name}`);
};

sayHi("Basil");
sayHi("Amaljith");
sayHi("Maneesha");
